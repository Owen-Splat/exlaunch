#pragma once
// #include "lib.hpp"
#include <cstring>
#include <string>

void install_blur_removal() {
    // std::string vanillaString = "PFXTiltShiftParam";
    // size_t maxlen = sizeof vanillaString;
    // // char* c = reinterpret_cast<char*>(0x8504000 + 0x16cbd73);
    // char* c = exl::reloc::GetLookupTable().SearchByName(vanillaString);
    // strncpy(c, "NoTiltShift", maxlen - 1);
    // c[maxlen] = '\0';
}